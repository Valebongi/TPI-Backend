server.port=8080
spring.application.name=servicio-administracion

# Configuración de la base de datos PostgreSQL (Docker)
spring.datasource.url=jdbc:postgresql://postgres:5432/tpi_backend_administracion_db
spring.datasource.username=tpi_user
spring.datasource.password=tpi_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate  
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Migración de esquema: actualizar esquema existente

# Configuración de Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging
logging.level.org.springframework.cloud=DEBUG
logging.level.utnfc.isi.back=DEBUG
